/**
 * Shared TypeScript types barrel.
 * `property.ts` holds the Property wire shape; `car.ts` the Car wire shape
 * (web + mobile consume them).
 */
export * from './property';
export * from './car';
