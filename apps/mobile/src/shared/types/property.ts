/**
 * Property wire types — canonical source: `@btlee/shared`.
 * Re-exported here so existing `@/shared/types/property` imports stay stable
 * (thin shim). Add new shared types to the package, not to this file.
 */
export type {
  DepositOption,
  FinishingType,
  GeoPoint,
  ListingType,
  Property,
  PropertyCategory,
  PropertyImage,
  PropertyOwner,
  PropertyService,
  PropertyStatus,
  PropertyType,
} from '@btlee/shared';
